<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up</title>
    <!-- Google Fonts for Poppins -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        /* Reset default styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Body styles */
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #1a1a1a;
            color: #ffffff;
            position: relative;
            overflow-x: hidden;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        /* Background shapes container */
        .background-shapes {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            overflow: hidden;
            backdrop-filter: blur(5px);
        }

        /* Flying shapes */
        .shape {
            position: absolute;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            animation: float 15s infinite linear;
        }

        .shape1 {
            width: 50px;
            height: 50px;
            top: 10%;
            left: 20%;
            animation-duration: 20s;
        }

        .shape2 {
            width: 30px;
            height: 30px;
            top: 50%;
            left: 70%;
            animation-duration: 25s;
        }

        .shape3 {
            width: 70px;
            height: 70px;
            top: 80%;
            left: 40%;
            animation-duration: 18s;
        }

        .shape4 {
            width: 40px;
            height: 40px;
            top: 30%;
            left: 90%;
            animation-duration: 22s;
        }

        .shape5 {
            width: 60px;
            height: 60px;
            top: 20%;
            left: 50%;
            animation-duration: 19s;
        }

        .shape6 {
            width: 20px;
            height: 20px;
            top: 70%;
            left: 10%;
            animation-duration: 23s;
        }

        .shape7 {
            width: 80px;
            height: 80px;
            top: 40%;
            left: 80%;
            animation-duration: 17s;
        }

        .shape8 {
            width: 35px;
            height: 35px;
            top: 60%;
            left: 30%;
            animation-duration: 21s;
        }

        .shape9 {
            width: 45px;
            height: 45px;
            top: 15%;
            left: 60%;
            animation-duration: 24s;
        }

        .shape10 {
            width: 55px;
            height: 55px;
            top: 85%;
            left: 85%;
            animation-duration: 20s;
        }

        /* Animation for flying shapes */
        @keyframes float {
            0% {
                transform: translate(0, 0);
            }

            50% {
                transform: translate(100vw, 100vh);
            }

            100% {
                transform: translate(0, 0);
            }
        }

        /* Main container */
        .main-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px;
            flex: 1;
            position: relative;
            z-index: 1;
        }

        /* Header styles */
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 0;
            position: relative;
        }

        .header::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 1px;
            background-color: rgba(255, 255, 255, 0.3);
        }

        .header-left {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .logo img {
            margin-top: -30px;
            margin-bottom: -40px;
            width: 150px;
            height: auto;
        }

        .company-name {
            font-size: 2.5rem;
            font-weight: 700;
            color: #ffffff;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .header-right .links {
            display: flex;
            gap: 15px;
        }

        /* Button styles */
        .btn {
            text-decoration: none;
            padding: 10px 25px;
            font-size: 1rem;
            font-weight: 600;
            border-radius: 25px;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

        .btn-primary {
            background-color: #ffffff;
            color: #1a1a1a;
            border: 2px solid #ffffff;
        }

        .btn-primary:hover {
            background-color: #e6e6e6;
            transform: scale(1.05);
        }

        .btn-secondary {
            background-color: transparent;
            color: #ffffff;
            border: 2px solid #ffffff;
        }

        .btn-secondary:hover {
            background-color: #ffffff;
            color: #1a1a1a;
            transform: scale(1.05);
        }

        /* Signup card */
        .signup-card {
            background: linear-gradient(145deg, #2c2c2c, #1f1f1f);
            border-radius: 15px;
            padding: 40px;
            max-width: 500px;
            margin: 50px auto;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.5);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            text-align: center;
            animation: fadeIn 1s ease-in-out;
        }

        .signup-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.7);
        }

        .signup-card h1 {
            font-size: 2.5rem;
            font-weight: 600;
            margin-bottom: 20px;
            color: #ffffff;
        }

        /* Form styles */
        .auth-form {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .auth-form input {
            padding: 12px 15px;
            font-size: 1rem;
            font-family: 'Poppins', sans-serif;
            background-color: #3a3a3a;
            border: 1px solid #4a4a4a;
            border-radius: 8px;
            color: #ffffff;
            outline: none;
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .auth-form input::placeholder {
            color: #b3b3b3;
        }

        .auth-form input:focus {
            border-color: #ffffff;
            box-shadow: 0 0 8px rgba(255, 255, 255, 0.2);
        }

        .auth-form button {
            padding: 12px;
            font-size: 1rem;
            font-weight: 600;
            background-color: #ffffff;
            color: #1a1a1a;
            border: none;
            border-radius: 25px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

        .auth-form button:hover {
            background-color: #e6e6e6;
            transform: scale(1.05);
        }

        /* Success and error messages */
        .success {
            color: #4caf50;
            font-size: 0.9rem;
            margin-top: 15px;
        }

        .error {
            color: #f44336;
            font-size: 0.9rem;
            margin-top: 15px;
        }

        /* Links below the form */
        .signup-card p {
            font-size: 0.9rem;
            color: #b3b3b3;
            margin-top: 20px;
        }

        .signup-card a {
            color: #ffffff;
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s ease;
        }

        .signup-card a:hover {
            color: #e6e6e6;
            text-decoration: underline;
        }

        /* Fade-in animation for the card */
        @keyframes fadeIn {
            0% {
                opacity: 0;
                transform: translateY(20px);
            }

            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Footer styles */
        footer {
            background-color: #2c2c2c;
            padding: 30px 0;
            text-align: center;
            margin-top: auto;
        }

        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
        }

        .footer-content p {
            font-size: 0.9rem;
            color: #b3b3b3;
            margin-bottom: 10px;
        }

        .footer-links {
            display: flex;
            justify-content: center;
            gap: 20px;
        }

        .footer-links a {
            color: #ffffff;
            text-decoration: none;
            font-size: 0.9rem;
            transition: color 0.3s ease;
        }

        .footer-links a:hover {
            color: #e6e6e6;
        }

        /* Responsive design */
        @media (max-width: 768px) {
            .company-name {
                font-size: 2rem;
            }

            .header {
                flex-direction: column;
                gap: 15px;
            }

            .header-right .links {
                flex-direction: column;
                gap: 10px;
            }

            .signup-card {
                max-width: 90%;
                padding: 30px;
            }
        }

        @media (max-width: 480px) {
            .company-name {
                font-size: 1.5rem;
            }

            .logo img {
                width: 50px;
            }

            .btn {
                padding: 8px 20px;
                font-size: 0.9rem;
            }

            .signup-card h1 {
                font-size: 2rem;
            }

            .auth-form input,
            .auth-form button {
                font-size: 0.9rem;
                padding: 10px;
            }
        }
    </style>
</head>

<body>
    <!-- Background with flying shapes -->


    <!-- Main container -->
    <div class="main-container">
        <!-- Header -->
        <header class="header">
            <div class="header-left">
                <div class="logo">
                    <img src="logoW.png" alt="MyCompany Logo">
                </div>
                <h1 class="company-name">THE LEARNING SPHERE</h1>
            </div>

        </header>

        <!-- Signup Card -->
        <div class="signup-card">
            <h1>Sign Up</h1>
            <form method="POST" action="/signup" class="auth-form">
                <input type="text" name="username" placeholder="Username" required>
                <input type="email" name="email" placeholder="Email" required>
                <input type="password" name="password" placeholder="Password" required>
                <div style="margin-top: 15px;">
                    <label style="font-size: 14px;">
                        <input type="checkbox" id="tos" required style="margin-right: 8px;">
                        I have read all the
                        <a href="/terms" style="color: rgb(29, 239, 232); text-decoration: underline;" target="_blank">Terms of
                            Service</a>
                    </label>
                </div>

                <button type="submit" class="btn">Sign Up</button>
            </form>
            {{#if error}}
            {{#if (eq error "Signup successful! Please check your email to verify your account.")}}
            <p class="success">{{error}}</p>
            {{else}}
            <p class="error">{{error}}</p>
            {{/if}}
            {{/if}}
            <p>Need to resend verification email? <a href="/resend-verification">Click here</a></p>
            <p>Already have an account? <a href="/login">Login</a></p>
        </div>
    </div>

    <!-- Footer -->
    <footer>
        <div class="footer-content">
            <p>© 2025 MyCompany. All rights reserved.</p>
            <div class="footer-links">
                <a href="/about">About</a>
                <a href="/contact">Contact</a>
                <a href="/privacy">Privacy Policy</a>
                <a href="/terms">Terms of Service</a>
            </div>
        </div>
    </footer>
</body>

</html>