<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{course.title}}</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Arial', sans-serif;
            background: #000;
            color: #fff;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            overflow-x: hidden;
        }
        .course-card {
            position: relative;
            width: 100%;
            max-width: 900px;
            background: #1a1a1a;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 20px 40px rgba(255, 255, 255, 0.1);
            display: flex;
            flex-direction: column;
            transition: transform 0.3s ease;
        }
        .course-card:hover {
            transform: translateY(-10px);
        }
        .course-image {
            position: relative;
            width: 100%;
            height: 300px;
            object-fit: cover;
            filter: opacity(1);
            transition: filter 0.3s ease;
        }
        .course-image:hover {
            filter:opacity(2);
        }
        .teacher-photo {
            position: absolute;
            top: 250px;
            right: 40px;
            width: 100px;
            height: 100px;
            border-radius: 50%;
            border: 4px solid #fff;
            filter: grayscale(100%);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.5);
            transition: transform 0.3s ease;
        }
        .teacher-photo:hover {
            transform: scale(1.1);
        }
        .course-content {
            padding: 60px 40px 40px;
            position: relative;
        }
        h1 {
            font-size: 40px;
            font-weight: bold;
            text-transform: uppercase;
            margin-bottom: 15px;
            background: -webkit-linear-gradient(#fff, #ccc);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        .description {
            font-size: 16px;
            opacity: 0.9;
            margin-bottom: 20px;
            line-height: 1.7;
        }
        .teacher-info {
            display: flex;
            align-items: center;
            gap: 15px;
            margin-bottom: 25px;
        }
        .teacher-name {
            font-size: 18px;
            font-weight: bold;
        }
        .details-grid {
            display: flex;
            justify-content: space-between;
            margin-bottom: 30px;
            font-size: 16px;
            background: rgba(255, 255, 255, 0.05);
            padding: 15px;
            border-radius: 10px;
        }
        .details-grid span {
            font-weight: bold;
            color: #ccc;
        }
        .enroll-btn {
            background: #fff;
            color: #000;
            padding: 12px 30px;
            font-size: 16px;
            font-weight: bold;
            text-transform: uppercase;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        .enroll-btn:hover {
            background: #000;
            color: #fff;
            box-shadow: 0 0 15px rgba(255, 255, 255, 0.5);
        }
        @media (max-width: 768px) {
            .course-card {
                width: 90%;
            }
            .course-image {
                height: 200px;
            }
            .teacher-photo {
                top: 150px;
                right: 20px;
                width: 80px;
                height: 80px;
            }
            .course-content {
                padding: 50px 20px 20px;
            }
            h1 {
                font-size: 32px;
            }
        }
    </style>
</head>
<body>
    <div class="course-card">
        <img src="/{{course.course_image}}" alt="Course Image" class="course-image">
        <img src="{{course.teacher_photo}}" alt="Teacher Photo" class="teacher-photo">
        <div class="course-content">
            <h1>{{course.title}}</h1>
            <p class="description">{{course.course_description}}</p>
            <div class="teacher-info">
                <div>
                    <span class="teacher-name">{{course.teacher_name}}</span>
                    <p style="font-size: 14px; opacity: 0.7;">Course Instructor</p>
                </div>
            </div>
            <div class="details-grid">
                <div><span>Start Date:</span> {{course.start_date}}</div>
                <div><span>Fees:</span> Rs.{{course.fees}}</div>
            </div>
            <button class="enroll-btn" onclick="enrollCourse('{{course.id}}')">Join Now</button>
        </div>
    </div>
    <script>
        function enrollCourse(courseId) {
            window.location.href = `/enroll/${courseId}`;
        }
    </script>
</body>
</html>